<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/strava_status_background"
    android:padding="16dp"
    android:layout_margin="8dp">

    <!-- Header with Strava Logo -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_strava_logo"
            android:background="@drawable/circle_background_orange"
            android:padding="6dp"
            android:layout_marginEnd="12dp"
            android:scaleType="centerInside" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Strava Connection"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?android:textColorPrimary" />

        <!-- Status Indicator -->
        <View
            android:id="@+id/strava_status_indicator"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/status_indicator_disconnected" />

    </LinearLayout>

    <!-- Status Text -->
    <TextView
        android:id="@+id/tv_strava_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Not connected to Strava"
        android:textSize="14sp"
        android:textColor="?android:textColorSecondary"
        android:layout_marginBottom="12dp" />

    <!-- Action Buttons Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <!-- Refresh Button -->
        <Button
            android:id="@+id/btn_refresh_strava"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Refresh"
            android:textColor="@color/strava_orange"
            android:layout_marginEnd="8dp"
            android:visibility="gone" />

        <!-- Sign Out Button -->
        <Button
            android:id="@+id/btn_signout_strava"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Disconnect"
            android:textColor="?android:textColorSecondary"
            android:layout_marginEnd="8dp"
            android:visibility="gone" />

        <!-- Connect Button -->
        <Button
            android:id="@+id/btn_connect_strava"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Connect"
            android:textColor="@android:color/white"
            android:background="@drawable/button_strava_orange_small"
            android:paddingHorizontal="16dp"
            android:textSize="14sp" />

    </LinearLayout>

</LinearLayout> 